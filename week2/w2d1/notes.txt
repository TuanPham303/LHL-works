- HTTP
  - need to require http first before using it: const http = require('http'). If it is https, use https

- ES6
  - const is used when the var is not gonna be changed in the future
  - let is used when the var is gonna be changed in the future

- read about dotenv npm and github api personal access token
  - to use the token, make a .env file and add 'GITHUB-ACCESS-TOKEN:your token'
